import { Dialog } from "@mui/material";

function DeleteTaskDialog({ open, onClose }) {
  return (
    <Dialog open={open} onClose={onClose}>
      123
    </Dialog>
  );
}

export default DeleteTaskDialog;
